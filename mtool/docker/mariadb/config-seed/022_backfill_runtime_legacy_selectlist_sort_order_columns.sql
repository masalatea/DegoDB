-- Generated by mtool/scripts/export_mtool_db_access_seed.php
-- Backfill SELECTLIST sort_order_columns from Project 1 legacy baseline for MTOOL

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ApacheHostSetting.CategoryName,ApacheHostSetting.VirtualHostName,ApacheHostSetting.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ApacheHostSetting'
  AND f.function_name = 'GetApacheHostSettingList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ApacheHostSetting.CategoryName,ApacheHostSetting.VirtualHostName,ApacheHostSetting.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ApacheHostSetting'
  AND f.function_name = 'GetMonitorLogList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ApacheHostSetting.CategoryName,ApacheHostSetting.VirtualHostName,ApacheHostSetting.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ApacheHostSetting'
  AND f.function_name = 'GetMonitorLogByProjectUserList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ApacheHostSetting.CategoryName,ApacheHostSetting.VirtualHostName,ApacheHostSetting.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ApacheHostSetting'
  AND f.function_name = 'GetAllList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ApacheHostSettingTemplate.name,ApacheHostSettingTemplate.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ApacheHostSettingTemplate'
  AND f.function_name = 'GetApacheHostSettingTemplateList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ApacheSetting.name,ApacheSetting.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ApacheSetting'
  AND f.function_name = 'GetApacheSettingList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'BuildTokenProjectSourceOutput.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'BuildTokenProjectSourceOutput'
  AND f.function_name = 'GetBuildTokenProjectSourceOutputList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'CompareOutputSearchCache.SearchKey_DropboxAccessToken,CompareOutputSearchCache.SearchKey_DropboxBaseFolderPath,CompareOutputSearchCache.SearchKey_DropboxFullPath,CompareOutputSearchCache.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'CompareOutputSearchCache'
  AND f.function_name = 'GetCompareOutputSearchCacheList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'DBBackup.SettingGroupPID,DBBackup.DropboxBaseFolderPID,DBBackup.ShellOutputFile,DBBackup.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'DBBackup'
  AND f.function_name = 'GetDBBackupList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'DBBackupUser.SettingGroupPID,DBBackupUser.DBBackupPID,DBConnection.DBName,DBUser.User,DBBackupUser.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'DBBackupUser'
  AND f.function_name = 'GetDBBackupUserList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'DBConnection.ServerPID,DBConnection.DBName,DBConnection.ObjectNameForPHP,DBConnection.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'DBConnection'
  AND f.function_name = 'GetDBConnectionList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'DBConnection.ServerPID,DBConnection.DBName,DBUser.User,DBUser.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'DBUser'
  AND f.function_name = 'GetDBUserList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'DBUserClientHost.DBUserPID,Server.LocalServerName'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'DBUserClientHost'
  AND f.function_name = 'GetDBUserClientHostList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'DBUserClientHost.DBUserPID,Server.LocalServerName'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'DBUserClientHost'
  AND f.function_name = 'GetDBUserClientHostByUserList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'DropboxSetting.name,DropboxSetting.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'DropboxSetting'
  AND f.function_name = 'GetDropboxSettingList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'LanguageResource.ProjectPID,LanguageResource.LanguageResourceGroupPID,LanguageResource.SortGroup,LanguageResource.KeyName,LanguageResource.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'LanguageResource'
  AND f.function_name = 'GetLanguageResourceList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'LanguageResourceGroup.Name,LanguageResourceGroup.ProjectPID,LanguageResourceGroup.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'LanguageResourceGroup'
  AND f.function_name = 'GetLanguageResourceGroupList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'LanguageResourceLang.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'LanguageResourceLang'
  AND f.function_name = 'GetLanguageResourceLangList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'LiveCheckTarget.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'LiveCheckTarget'
  AND f.function_name = 'GetLiveCheckTargetList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Project.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Project'
  AND f.function_name = 'GetProjectList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Project.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Project'
  AND f.function_name = 'GetProjectbyOwnerOrUserSecurityList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ProjectGroup.CreatedDateTime'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ProjectGroup'
  AND f.function_name = 'GetProjectGroupList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ProjectGroupTemplate.ProjectGroupType'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ProjectGroupTemplate'
  AND f.function_name = 'GetProjectGroupTemplateList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ProjectSecurityForEachPage.SERVER_NAME,ProjectSecurityForEachPage.SCRIPT_NAME'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ProjectSecurityForEachPage'
  AND f.function_name = 'GetProjectSecurityForEachPageList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ProjectSourceOutput.ProjectSourceOutputListOrder,ProjectSourceOutput.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ProjectSourceOutput'
  AND f.function_name = 'GetProjectSourceOutputList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'ProjectUser.username'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'ProjectUser'
  AND f.function_name = 'GetProjectUserList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Req.UpdatedDateTime desc,Req.AddedDateTime desc'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Req'
  AND f.function_name = 'GetReqList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Req.UpdatedDateTime desc,Req.AddedDateTime desc'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Req'
  AND f.function_name = 'GetReqOfTargetProjectList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Server.LocalServerName,Server.IP,Server.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Server'
  AND f.function_name = 'GetServerList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Server.LocalServerName,Server.IP,Server.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Server'
  AND f.function_name = 'GetSpecificOrAnyCommonServerList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Server.IsCommonForAllSettingGroupForUpload desc,Server.LocalServerName,Server.IP,Server.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Server'
  AND f.function_name = 'GetSpecificOrCommonUploadServerList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Server.IsCommonForAllSettingGroupForDBServer desc,Server.LocalServerName,Server.IP,Server.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Server'
  AND f.function_name = 'GetSpecificOrCommonDBServerList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Server.IsCommonForAllSettingGroupForDBClient desc,Server.LocalServerName,Server.IP,Server.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Server'
  AND f.function_name = 'GetSpecificOrCommonDBClientList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Spec.name,Spec.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Spec'
  AND f.function_name = 'GetSpecList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Spec.name,Spec.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Spec'
  AND f.function_name = 'GetSpecByOwnerOrUserSecurityList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'SpecContent.ContentOrder,SpecContent.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'SpecContent'
  AND f.function_name = 'GetSpecContentList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'SpecialHoliday.year'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'SpecialHoliday'
  AND f.function_name = 'GetYearListList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'SpecialHoliday.year,SpecialHoliday.month,SpecialHoliday.day'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'SpecialHoliday'
  AND f.function_name = 'GetDaysInYearList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'TestCondition.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'TestCondition'
  AND f.function_name = 'GetTestConditionList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'TestConditionSelection.SelectionOrder,TestConditionSelection.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'TestConditionSelection'
  AND f.function_name = 'GetTestConditionSelectionList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'TestConditionSelection.SelectionOrder,TestConditionSelection.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'TestConditionSelection'
  AND f.function_name = 'GetNewestTestConditionSelectionList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'TestConditionSelection.SelectionOrder,TestConditionSelection.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'TestConditionSelection'
  AND f.function_name = 'GetNewestOrHasResultTestConditionSelectionList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'TestGroup.name,TestGroup.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'TestGroup'
  AND f.function_name = 'GetTestGroupList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'TestGroup.name,TestGroup.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'TestGroup_leftouterjoin_Project'
  AND f.function_name = 'GetTestGroupByOwnerOrUserSecurityList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'TestPattern.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'TestPattern'
  AND f.function_name = 'GetTestPatternList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'TestPatternSelection.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'TestPatternSelection'
  AND f.function_name = 'GetTestPatternSelectionList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'Test.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'Test_leftouterjoin_Project'
  AND f.function_name = 'GetTestList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'UploadDropboxPathCacheItems.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'UploadDropboxPathCacheItems'
  AND f.function_name = 'GetUploadDropboxPathCacheItemsList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'UploadGroup.ListOrder,UploadGroup.Name,UploadGroup.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'UploadGroup'
  AND f.function_name = 'GetUploadGroupList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'UploadGroup.ListOrder,UploadGroup.Name,UploadGroup.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'UploadGroup'
  AND f.function_name = 'GetUploadGroupByUserList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'UploadGroupAssignedServerPath.ListOrder,Server.LocalServerName,UploadServerPath.DropboxPath,UploadGroupAssignedServerPath.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'UploadGroupAssignedServerPath'
  AND f.function_name = 'GetUploadGroupAssignedServerPathList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'UploadGroupAssignedServerPath.ListOrder,Server.LocalServerName,UploadServerPath.DropboxPath,UploadGroupAssignedServerPath.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'UploadGroupAssignedServerPath'
  AND f.function_name = 'GetUploadGroupAssignedServerPathByUserAndGroupList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'UploadServerPath.CategoryName,DropboxSetting.name,DropboxBaseFolder.Name,Server.LocalServerName,UploadServerPath.DropboxPath,UploadServerPath.LocalPath'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'UploadServerPath'
  AND f.function_name = 'GetUploadServerPathList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'UploadServerPath.CategoryName,DropboxSetting.name,DropboxBaseFolder.Name,Server.LocalServerName,UploadServerPath.DropboxPath,UploadServerPath.LocalPath'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'UploadServerPath'
  AND f.function_name = 'GetAllUploadServerPathList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'UploadServerPath.CategoryName,DropboxSetting.name,DropboxBaseFolder.Name,Server.LocalServerName,UploadServerPath.DropboxPath,UploadServerPath.LocalPath'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'UploadServerPath'
  AND f.function_name = 'GetUploadServerPathForUserList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'chattopic.IsOld,chattopic.name,chattopic.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'chattopic'
  AND f.function_name = 'GetchattopicList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'chattopicAttachment.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'chattopicAttachment'
  AND f.function_name = 'GetchattopicAttachmentList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'chattopic.IsOld,chattopic.name,chattopic.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'chattopic_and_Project'
  AND f.function_name = 'GetchattopicList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'da.name,da.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'da'
  AND f.function_name = 'GetdaList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'daCustomProxyFunc.FunctionListOrder,daCustomProxyFunc.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'daCustomProxyFunc'
  AND f.function_name = 'GetdaCustomProxyFuncList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'daCustomProxyFunc.FunctionListOrder,daCustomProxyFunc.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'daCustomProxyFunc_leftouterjoin_dafunc_and_da'
  AND f.function_name = 'GetAlldaCustomProxyFunc_leftouterjoin_dafunc_and_da_ForProjectSourceOutputList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'daCustomProxyFunc.FunctionListOrder,daCustomProxyFunc.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'daCustomProxyFunc_leftouterjoin_dafunc_and_da'
  AND f.function_name = 'GetdaCustomProxyFunc_leftouterjoin_dafunc_and_daList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dafunc.FunctionListOrder,dafunc.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dafunc'
  AND f.function_name = 'GetdafuncList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dafuncinserttargetfields.FieldListOrder,dafuncinserttargetfields.ProjectPID,dafuncinserttargetfields.daPID,dafuncinserttargetfields.dafuncPID,dafuncinserttargetfields.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dafuncinserttargetfields'
  AND f.function_name = 'GetdafuncinserttargetfieldsList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dafuncselecthaving.HavingListOrder,dafuncselecthaving.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dafuncselecthaving_leftouterjoin_targetfields'
  AND f.function_name = 'GetdafuncselecthavingList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dafuncselecttargetfields.FieldListOrder,dafuncselecttargetfields.targetTableName,dafuncselecttargetfields.targetTableAliasName,dafuncselecttargetfields.targetTableColumnName,dafuncselecttargetfields.storeClassFieldName,dafuncselecttargetfields.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dafuncselecttargetfields'
  AND f.function_name = 'GetdafuncselecttargetfieldsList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dafuncselectwhere.WhereOrder,dafuncselectwhere.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dafuncselectwhere'
  AND f.function_name = 'GetdafuncselectwhereList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dafuncupdatedeletewhere.WhereOrder,dafuncupdatedeletewhere.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dafuncupdatedeletewhere'
  AND f.function_name = 'GetdafuncupdatedeletewhereList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dafuncupdatetargetfields.FieldListOrder,dafuncupdatetargetfields.ProjectPID,dafuncupdatetargetfields.daPID,dafuncupdatetargetfields.dafuncPID,dafuncupdatetargetfields.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dafuncupdatetargetfields'
  AND f.function_name = 'GetdafuncupdatetargetfieldsList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dataclass.name'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dataclass'
  AND f.function_name = 'GetdataclassList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dataclassfields.FieldListOrder,dataclassfields.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dataclassfields'
  AND f.function_name = 'GetdataclassfieldsList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dbtable.name'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dbtable'
  AND f.function_name = 'GetdbtableList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'dbtablecolumns.ColumnListOrder,dbtablecolumns.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'dbtablecolumns'
  AND f.function_name = 'GetdbtablecolumnsList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'htmlTemplate.name,htmlTemplate.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'htmlTemplate_leftouterjoin_ParentHtmlTemplate'
  AND f.function_name = 'GethtmlTemplateByTargetTypeList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'htmlTemplate.name,htmlTemplate.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'htmlTemplate_leftouterjoin_ParentHtmlTemplate'
  AND f.function_name = 'GethtmlTemplateList'
  AND f.action_type = 'SELECTLIST';

UPDATE project_db_access_functions AS f
INNER JOIN project_db_access_classes AS c
    ON c.id = f.db_access_class_id
INNER JOIN projects AS p
    ON p.id = c.project_id
SET
    f.sort_order_columns = IF(
        f.source_of_truth = 'manual' AND f.sort_order_columns <> '',
        f.sort_order_columns,
        'html.ProjectSourceOutputPID,html.name,html.PID'
    ),
    f.source_of_truth = IF(f.source_of_truth = 'manual', f.source_of_truth, 'seed-legacy'),
    f.updated_at = CURRENT_TIMESTAMP
WHERE p.project_key = 'MTOOL'
  AND c.source_name = 'html_leftouterjoin_htmlTemplate'
  AND f.function_name = 'GethtmlList'
  AND f.action_type = 'SELECTLIST';
