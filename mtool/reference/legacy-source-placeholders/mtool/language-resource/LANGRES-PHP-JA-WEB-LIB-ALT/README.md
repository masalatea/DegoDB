# Placeholder

This placeholder bundle stands in for legacy `ProjectSourceOutput.PID=369`
(` /legacy/ftp/www/ja.matsuesoft.com/lib `).

- The new runtime must not read `original-codes/` directly.
- This legacy target dir overlaps `PSO 279`, so the bridge keeps a separate source output key.
- Replace this placeholder with a new Mtool-generated language resource output.
