# Sample26 HTML Page Module

This curated module is the HTML-PAGE source for `sample26-ebook-headless-cms-capstone`.
It is intentionally static sample output: the runtime exercise is publishing a reader-site
artifact from Mtool metadata while the same project also exposes app API metadata,
ProjectToken editor proxy endpoints, and a project metadata bundle.
