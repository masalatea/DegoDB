# SAMPLE11 HTML-PAGE

This is the curated current HTML module source for `SAMPLE11/HTML-PAGE`.

It is intentionally small so the tutorial can focus on the `html-module-catalog` Source Output flow without depending on legacy `original-codes/`.
