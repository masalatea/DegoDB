# sample53-runtime-whiteboard

- canonical project key: `SAMPLE4`
- legacy reference: `Project.PID = 12` (`Whiteboard`)
- 役割: collaboration-style metadata を見るための sample pack
- seed には representative な legacy `project_source_outputs` metadata を少量含む
- durable input: `seed/`
- disposable runtime root: `work/sample-packs/sample53-runtime-whiteboard/`

起動:

```bash
./sample/legacy-projects/sample53-runtime-whiteboard/run.sh up
```
