# patterns

- 役割: historical transitional root
- active pack は `sample/tutorials/` と `sample/internal-patterns/` へ移動した
- 新しい sample をここへ追加しない

現時点でここに残るものは historical leftover のみであり、current sample pack catalog / current test entry / current helper default からは参照しない。
