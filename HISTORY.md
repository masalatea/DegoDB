# History

## Ver 1.0.0 - 2026-06-14

Initial remake baseline for DegoDB.

### Included

- Current `mtool/` runtime, generator, shared app code, and scripts.
- Docker-based local development environment for admin, lab, config DB, and lab DB.
- Canonical metadata import, sync, and source-output generation workflow.
- Tutorial sample lane from simple table output through mini CRUD flow.
- Internal pattern samples and legacy-reference snapshots for rewrite guards.
- PHPUnit integration tests and scenario checks.
- Permanent documentation under `docs/` and dated work reports under `docs/reports/`.

### Notes

- This version is the first squashed project baseline for the remake.
- Older upstream history exists outside this remake baseline and is intentionally not replayed here.
- Local-user absolute paths and old commit identity metadata were scrubbed before release.
- Legacy service names, domains, and identifiers remain where they are part of historical reference material or compatibility naming.
